async function jsonToObject(json) {
  /*console.log(json.data.days[1].datetime);
  /*const status = await json.days[0].description;
  const condition = await json.days[0].conditions;
  const tempmax = await json.days[0].tempmax;
  const tempmin = await json.days[0].tempmin;
  const precipitationProbability = await json.data.days[0].precipprob;

  return { status, condition, tempmax, tempmin, precipitationProbability };
*/
}

export { jsonToObject };
